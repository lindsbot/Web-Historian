exports.readUrls = function(filePath, cb){
  // go to file named by filePath
  // run cb on each url
};

exports.downloadUrls = function(urls){
    // read url list
    // for each url, visit site, get page source
    // stick page source in /sites/
};
